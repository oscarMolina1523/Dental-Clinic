export enum InventoryMovementStatus {
  ENTRY = "ENTRY",
  EXIT = "EXIT",
  ADJUSTMENT_IN = "ADJUSTMENT_IN", //ajuste de entrada
  ADJUSTMENT_OUT = "ADJUSTMENT_OUT", //ajuste de salida
  RETURN = "RETURN", //devuelto
  LOSS = "LOSS", //perdido
  EXPIRED = "EXPIRED",
}